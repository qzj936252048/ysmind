$(function () {
	$.extend($.fn.validatebox.defaults.rules, {//添加验证规则
	     floatNumber: {//value值为文本框中的值
	           validator: function (value) {
	          var reg = /^\d+(\.\d+)?$/;
	           return reg.test(value);
	       },
	       message: '输入内容只能是数字'
	  }
	});
});
